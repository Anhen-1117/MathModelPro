# MathModelPro 环境变量配置

# Redis（可选，不配置则使用内存模式）
REDIS_URL=redis://localhost:6379/0

# Tavily Web 搜索（可选，免费注册：https://tavily.com）
TAVILY_API_KEY=

# Semantic Scholar（可选，免费，无需 API Key）
# 自动使用公共 API

# OpenAlex（可选，用于文献检索）
# 设置邮箱可获得更快的 API 响应
OPENALEX_EMAIL=
